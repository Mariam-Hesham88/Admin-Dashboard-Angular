import { SearchTaskItemPipe } from './search-task-item-pipe';

describe('SearchTaskItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTaskItemPipe();
    expect(pipe).toBeTruthy();
  });
});
